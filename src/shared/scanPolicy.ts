/** Validité affichée du ticket client (preuve en caisse). */
export const TICKET_VALIDITY_MS = 60 * 60 * 1000;

/** Date sentinelle : blocage appareil permanent par restaurant (device_blocks). */
export const DEVICE_BLOCK_PERMANENT_UNTIL_ISO = '2099-12-31T23:59:59.999Z';
